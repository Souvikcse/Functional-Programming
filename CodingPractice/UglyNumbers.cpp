#include<iostream>
#include<vector>
#include<algorithm>
#include<unordered_map>
typedef long long int ll;
using namespace std;

vector<ll> v;
void find(int i, int n, int num){
    if(v.size()==n)
        return;
    
}
int main(){
    int t;
    cin>>t;
    find(0, 10000);
    sort(v.begin(), v.end());
    while(t--){
        int n;
        cin>>n;
    }
}